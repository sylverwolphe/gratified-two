name = "gratified-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Custom domain: api.gratified.com
# Configure via Cloudflare dashboard after deployment

[vars]
ENVIRONMENT = "sandbox"

# Set these as secrets via `wrangler secret put`:
# wrangler secret put SQUARE_ACCESS_TOKEN
# wrangler secret put SQUARE_LOCATION_ID
